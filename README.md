# Metro Riyadh CRUD Interface

This is a code bundle for Metro Riyadh CRUD Interface. The original project is available at:
https://www.figma.com/design/eamnz7HA1BaLTIUqq47ses/Metro-Riyadh-CRUD-Interface

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

